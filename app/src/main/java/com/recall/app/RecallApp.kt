package com.recall.app

import android.app.Application

/**
 * Declared in AndroidManifest.xml as android:name=".RecallApp".
 * Nothing to do here yet — it exists so there is one obvious place to put
 * app-wide setup later (logging, crash reporting, and so on).
 */
class RecallApp : Application()
